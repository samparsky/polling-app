#!/usr/bin/env node

const program = require("commander")

program
  .version('0.1.0')
  .command('create_organisation <dir>')
  .command('add_constituent')
  .command('remove_constituent')
  .command('add_initiative')
  .command('vote')
  .command('get_result')



function createOrganisation() {
  const tx = //
  
  // sign 

  // broadcast
}